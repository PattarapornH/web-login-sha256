extends layout

block content
    br
    h1= article.title + "\'s Medal"
    br
    table.table
        thead.thead-dark
            tr
                th Gold
                th Silver
                th Bronze
        tbody
            tr
                td #{article.gold}
                td #{article.silver}
                td #{article.bronze}

    hr
    if user
        //- if user.id ==article.author
        br
        a.btn.btn-default(href='/articles/edit/'+article._id) Update 
        a.btn.btn-danger.delete-article(href='#',data-id=article._id) Delete
  
